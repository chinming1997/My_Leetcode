int *ptr = lower_bound(陣列, 陣列+長度, 查找值);//用於排序過後的遞增array
if (ptr == 陣列+長度|| *ptr != 查找值)
{
   ptr會回傳查找值的位址，如果在陣列尾端代表沒有這個值
}

vector<int> n[maxn]; maxn長度的二維陣列 for (auto num : nums) 取vector的值

isalpha、tolower

for(set<string>::iterator it = dic.begin();it!=dic.end();it++) cout<<*it;

leetcode single number 利用計算機按位儲存數字的特性來找只出現一次的 => XOR 某數出現兩次會是0
一個數xor 0還會是那個數字本身
一個數字xor自己，會等於0